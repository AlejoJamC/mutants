ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.conekta.mutants.MutantAppKt.module ]
    }
}

mutant {
    version = local
    locale = es
    countryCode = co
}
